# Zig build artifacts
.zig-cache/
zig-out/
# Release bundles (scripts/make_dist.mjs) + cached Zig toolchain downloads.
.dist/
.dist-cache/
# Local-only workload material and ops scripts (kept out of the public repo).
/testSQL/
/dave/
tests/bun/_gen_cdc.mjs
tests/bun/_sync_watchdog.mjs
tests/bun/_project_backfill.mjs
tests/bun/_bulk_load.mjs
tests/bun/_tvf_ride_probe.mjs
bench/_*.mjs
# Session scratch: server logs, probe output, local bench databases.
/*.log
/_*.out
/_*.err
/_bench_bin/
/_prod_bin/
/.*-db/
/.*-db2/
.zig-global-cache*/
.tools/
# Local Zig package cache (populated by build.zig.zon deps).
zig-pkg/
# Throwaway clone we used while debugging the zstd Windows-symlink issue.
vendor/zstd-upstream/

# Bench scratch directories — recreated each run
.bench-data/
.clickbench-db/
# ClickBench TSV dataset — multi-GB, regenerated on demand
bench/clickbench/data/

# Local IDE / editor files
.vscode/
.idea/
*.swp
*~

# OS junk
.DS_Store
Thumbs.db

# Personal Claude/agent caches (in case anything lands inside the repo)
.claude/

# Local server/test scratch
.workbench-db/
.workbench-server.*.log
bun-tmp-test/

# Scratch files from one-time refactor passes
*.body

# DuckDB comparison harness — multi-GB DB + generated query outputs (regenerable)
bench/clickbench/duckdb/
# ClickBench coverage/standing captures (regenerated by the bench runner)
bench/clickbench/coverage*.json
# Root-level throwaway microbenches + perf captures (zig run scratch)
/keybench.zig
/distinctbench.zig
/lazybench.zig
/compactbench.zig
/formicro.zig
/aggbench.zig
/computebench.zig
/membench.zig
/_sz_scratch.zig
/thindb_now.json
/thindb_per*.json
/thindb_run.txt
/q33.json.gz
# Bun bench scratch
/tests/bun/ab_base.json
/tests/bun/losing.json
/tests/bun/rawv9.json
/tests/bun/q15probe.ts
/tests/bun/q40probe.ts
# Stray build/server logs
*.log
/build_err.txt
tests/flink/_checkpoints/
.wayroll-prod-db/

# deployment-specific CDC material (customer schemas, prod job config)
_cdc_rig/consolidated/
_cdc_rig/box-scripts/

# flink-cdc integration build artifacts
integrations/flink-cdc/target/
integrations/flink-cdc/dependency-reduced-pom.xml
/_conformance_results.jsonl
